Problem statement

    - what will happen in the future / chart

Disclaimier

    - intel
        rdpmc
        intel-pt
        ...
    - linux
    - ...

    powered by
        https://github.com/perf-labs/perf (perf + extenions)

        supports any language C++, rust, zig, ...

    perf info

Approaches
    - top down analysis
    - IPC
    - instrumenting...
    - sampling...
    - ...
        - may require multiple runs

What
    latency!
        - you wanna be fast you wanna be in the future speculative [TIP]

Profiling -> Benchmarking

    region
        - profile regions not functions (inlining, setup)
    inline
        - inline function (different version each time)

    -> use the same binary

    profiling is good to get actual distrbutions of data and cache misses


Bias (controlled chaos)

    Cache:
        prefetch/cldemote
    Branch:
        different data each iteration
         - fwd no-taken
         - bkw taken
         - never-taken (no entry in BTB)
    Layout:
        reshuffle
    Others:
        TLB, port executions, ROB, ...

    Questions:
        - what about noise enviornments

    perf tune latency

Examples
    branching: fizz_buzz
    hash table vs lookup table (mph) - cache (fpga)
    cache: dispatching table or branchless binary
    layout: unordered_map
    simd - throughput (gpu, tpu)

     - ecdf (doesn't lose value)

How (symbolic execution)

    [how]
        states

    regions

        perf bench func fizz_buzz
        perf bench region hot_begin..hot_end
        perf bench asm 'mov eax, 42' --setup 'mov eax, 0'
        perf bench func fizz_buzz --setup-region h..dd --teardown-asm 'mov eax, 42'

Verification
    perf.bench(asm).instrucitons < 10

Further readings
    Intel Manual
  - angr/mca/uica
